import type { CustomThemeConfig } from 'tailwindcss/types/config'

/**
 * Type of the `colors` or `theme.extend.colors` property in a Tailwind CSS configuration object.
 */
export type TailwindColorsConfig = CustomThemeConfig['colors']
